---
title:
  de: "SKILL.md & Trigger"
  en: "SKILL.md & Triggers"
researchedOn: 2026-05-06
sources:
  - https://code.claude.com/docs/en/slash-commands
  - https://code.claude.com/docs/en/agent-sdk/skills
---

# <I18n de="Anatomie eines Skills" en="Anatomy of a Skill" />

<De>
## Verzeichnis-Struktur

```
.claude/skills/
└── pr-review/
    ├── SKILL.md           # Pflicht: Definition
    ├── checklist.md       # optional: Helper-Doku
    └── scripts/
        └── extract-diff.sh
```

## SKILL.md-Aufbau

<CommandBox lang="markdown">
{`---
name: pr-review
description: |
  Review a GitHub pull request systematically. Use when the user
  shares a PR URL or types "review PR #123".
allowed-tools:
  - Bash(gh pr view *)
  - Bash(gh pr diff *)
  - Read
disable-model-invocation: true
---

# PR Review Skill

## Step 1: Fetch the PR
\`\`\`bash
gh pr view $ARGUMENTS --json title,body,files,commits
\`\`\`

## Step 2: Read the diff
Apply the checklist in checklist.md (security, correctness, simplicity, tests).

## Step 3: Comment
Post structured review via \`gh pr review\`.`}
</CommandBox>

## Frontmatter-Felder

| Feld | Pflicht | Was es macht |
|---|---|---|
| `name` | ✓ | Skill-ID, auch Slash-Command-Name |
| `description` | ✓ | Trigger-Text — sollte typische User-Anfragen enthalten |
| `allowed-tools` | optional | Erlaubt diese Tools ohne per-use Approval, wenn Skill aktiv ist |
| `disable-model-invocation` | optional | verhindert Auto-Invoke; nur User kann `/<skill>` starten |
| `user-invocable` | optional | `false` = nur Claude kann den Skill nutzen |
| `model` | optional | Modell-Hinweis für diesen Skill, wenn unterstützt |

<NoteCard variant="tip" title="Goldene Regel für Trigger">
Im `description` nicht nur sagen WAS der Skill tut, sondern auch HOW USERS ASK. Für Deploy/Commit/Slack-Post: `disable-model-invocation: true`.
</NoteCard>
</De>

<En>
## Folder structure

```
.claude/skills/
└── pr-review/
    ├── SKILL.md           # required: definition
    ├── checklist.md       # optional: helper doc
    └── scripts/
        └── extract-diff.sh
```

## SKILL.md anatomy

<CommandBox lang="markdown">
{`---
name: pr-review
description: |
  Review a GitHub pull request systematically. Use when the user
  shares a PR URL or types "review PR #123".
allowed-tools:
  - Bash(gh pr view *)
  - Bash(gh pr diff *)
  - Read
disable-model-invocation: true
---

# PR Review Skill

## Step 1: Fetch the PR
\`\`\`bash
gh pr view $ARGUMENTS --json title,body,files,commits
\`\`\`

## Step 2: Read the diff
Apply the checklist in checklist.md (security, correctness, simplicity, tests).

## Step 3: Comment
Post a structured review via \`gh pr review\`.`}
</CommandBox>

## Frontmatter fields

| Field | Required | What it does |
|---|---|---|
| `name` | ✓ | Skill ID, also slash-command name |
| `description` | ✓ | Trigger text — should contain typical user phrasings |
| `allowed-tools` | optional | Grants these tools without per-use approval while the skill is active |
| `disable-model-invocation` | optional | prevents auto-invoke; only the user can run `/<skill>` |
| `user-invocable` | optional | `false` = only Claude can use the skill |
| `model` | optional | model hint for this skill, when supported |

<NoteCard variant="tip" title="Golden rule for triggers">
In `description` don't just say WHAT the skill does, also HOW USERS ASK. For deploy/commit/Slack-post workflows: `disable-model-invocation: true`.
</NoteCard>
</En>

<SpeakerNotes>
<I18n
  de="$ARGUMENTS hervorheben — das ist die User-Eingabe nach `/pr-review 42`. allowed-tools ist Convenience + Risiko: es reduziert Prompts für genau diese Tools, ersetzt aber kein Permission-Design. Live: Skill installieren via cp ins .claude/skills, dann /skills aufrufen, Trigger sollte sichtbar sein."
  en="Highlight $ARGUMENTS — that's the user input after `/pr-review 42`. allowed-tools is convenience + risk: it reduces prompts for exactly these tools, but it is not a replacement for permission design. Live: install skill via cp into .claude/skills, then run /skills, trigger should be visible."
/>
</SpeakerNotes>
